import { TaskFormComponent } from './task-form.component';
import { Routes } from '@angular/router';

export const taskFormRouting: Routes = [
  {
    path: '',
    component: TaskFormComponent,
  },
]
